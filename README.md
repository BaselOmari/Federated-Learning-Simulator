# Federated Learning Simulator
Simulating FedAvg and the Federated Learning protocol to train a CNN against the MNIST dataset.

Simulation is multithreaded where each client is housed in a separate thread.
